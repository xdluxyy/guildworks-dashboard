<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>GuildWorks Dashboard</title>
  <style>
    /* === Reset and Base === */
    body {
      margin: 0;
      padding: 0;
      font-family: "Poppins", sans-serif;
      background-color: #0b0c10;
      color: white;
      overflow-x: hidden;
    }

    /* === Background Dots === */
    body::before {
      content: "";
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: radial-gradient(rgba(255, 255, 255, 0.07) 1px, transparent 1px);
      background-size: 30px 30px;
      z-index: 0;
    }

    /* === Header === */
    header {
      width: 100%;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 18px 60px;
      position: fixed;
      top: 0;
      left: 0;
      background: rgba(10, 10, 15, 0.65);
      backdrop-filter: blur(10px);
      border-bottom: 1px solid rgba(255, 255, 255, 0.05);
      z-index: 10;
    }

    .logo {
      font-weight: 600;
      font-size: 1.3rem;
      color: #fff;
    }

    .logo span {
      color: #5865F2;
      font-weight: 700;
    }

    nav a {
      margin: 0 18px;
      text-decoration: none;
      color: #bdbdbd;
      font-weight: 500;
      transition: color 0.2s ease;
    }

    nav a:hover {
      color: #ffffff;
    }

    .dash-btn {
      padding: 10px 20px;
      background: linear-gradient(120deg, #5865f2, #7289da);
      border-radius: 10px;
      color: white;
      font-weight: 600;
      text-decoration: none;
      transition: all 0.3s ease;
    }

    .dash-btn:hover {
      background: linear-gradient(120deg, #4752c4, #5865f2);
      box-shadow: 0 0 15px rgba(88, 101, 242, 0.7);
    }

    /* === Center Content === */
    .content {
      position: relative;
      z-index: 2;
      text-align: center;
      margin-top: 180px;
      padding: 60px 30px 180px;
    }

    .content h1 {
      font-size: 2.4rem;
      font-weight: 700;
      color: white;
    }

    .content h1 span {
      color: #5865f2;
    }

    .content p {
      color: #bdbdbd;
      font-size: 1.05rem;
      margin-top: 15px;
      line-height: 1.6;
    }

    /* === Footer === */
    footer {
      width: 100%;
      text-align: center;
      padding: 15px 0;
      font-size: 0.9rem;
      color: #bdbdbd;
      background: rgba(10, 10, 15, 0.65);
      backdrop-filter: blur(10px);
      border-top: 1px solid rgba(255, 255, 255, 0.05);
      position: fixed;
      bottom: 0;
      left: 0;
      z-index: 10;
    }

    footer a {
      color: #5865f2;
      text-decoration: none;
      margin: 0 8px;
    }

    footer a:hover {
      text-decoration: underline;
    }

    /* === Responsiveness === */
    @media (max-width: 768px) {
      header {
        flex-direction: column;
        padding: 15px;
        gap: 10px;
      }

      nav a {
        margin: 0 10px;
      }

      .content {
        margin-top: 220px;
        padding: 50px 20px 180px;
      }

      .content h1 {
        font-size: 1.8rem;
      }
    }
  </style>
</head>
<body>
  <!-- HEADER -->
  <header>
    <div class="logo">Guild<span>Works</span></div>
    <nav>
      <a href="#">Home</a>
      <a href="#">Features</a>
      <a href="#">Support</a>
      <a href="#" class="dash-btn">Dashboard</a>
    </nav>
  </header>

  <!-- MAIN CONTENT -->
  <div class="content">
    <h1>The <span>PREMIER</span> ticket solution for your community.</h1>
    <p>We provide expert ticket management designed to simplify your workflow, streamline communication, and ensure every request receives the care and efficiency it deserves.</p>
    <p>Our dashboard is coming soon. Check back here later...</p>
  </div>

  <!-- FOOTER -->
  <footer>
    © 2025 GuildWorks Discord Solutions — Powered by xdluxyy • 
    <a href="#">Privacy Policy</a> • 
    <a href="#">Terms of Service</a>
  </footer>
</body>
</html>
