<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>GuildWorks</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <!-- Navigation -->
    <header class="navbar">
      <div class="logo">
        <img src="/img/gw-logo.png" alt="GuildWorks Logo" class="logo-img" />
        <span class="logo-text">Guild<span class="accent">Works</span></span>
      </div>
      <nav>
        <ul>
          <li><a href="#">Home</a></li>
          <li><a href="#">Features</a></li>
          <li><a href="#">Support</a></li>
        </ul>
      </nav>
      <a href="#" class="dashboard-btn">Dashboard</a>
    </header>

    <!-- Hero / Premier Section -->
    <main>
      <section class="hero">
        <div class="dot-field"></div>
        <div class="holo-box">
          <h1>
            The <span class="accent">PREMIER</span> ticket solution for your community.
          </h1>
          <p>
            We provide expert ticket management designed to simplify your workflow, streamline communication,
            and ensure every request receives the care and efficiency it deserves.
          </p>
          <p>Our dashboard is coming soon. Check back here later…</p>
        </div>
      </section>
    </main>

    <!-- Footer -->
    <footer>
      <div class="footer-container">
        <div class="footer-brand">
          <h2>GUILD<span class="accent">WORKS</span></h2>
          <p>© 2025 GuildWorks Discord Solutions</p>
          <p>Powered by xdluxyy</p>
        </div>

        <div class="footer-links">
          <div>
            <h3>NAVIGATION</h3>
            <a href="#">Home</a>
            <a href="#">Services</a>
            <a href="#">Support</a>
          </div>
          <div>
            <h3>IMPORTANT</h3>
            <a href="#">Terms of Service</a>
            <a href="#">Privacy Policy</a>
          </div>
          <div>
            <h3>PRODUCTS</h3>
            <a href="#">Ticket System</a>
            <a href="#">Verification</a>
            <a href="#">Workflow Tools</a>
          </div>
        </div>
      </div>

      <div class="footer-bottom">
        <p>
          © 2025 GuildWorks — Powered by xdluxyy •
          <a href="#">Privacy Policy</a> •
          <a href="#">Terms of Service</a>
        </p>
      </div>
    </footer>

    <script>
      const dotField = document.querySelector(".dot-field");
      const ctx = document.createElement("canvas");
      const c = ctx.getContext("2d");
      dotField.appendChild(ctx);

      function resize() {
        ctx.width = window.innerWidth;
        ctx.height = 400;
      }
      resize();
      window.addEventListener("resize", resize);

      const dots = Array(180)
        .fill()
        .map(() => ({
          x: Math.random() * ctx.width,
          y: Math.random() * ctx.height,
          size: 1,
          baseY: 0,
        }));

      window.addEventListener("mousemove", (e) => {
        const rect = ctx.getBoundingClientRect();
        const mx = e.clientX - rect.left;
        const my = e.clientY - rect.top;
        dots.forEach((d) => {
          const dx = mx - d.x;
          const dy = my - d.y;
          const dist = Math.sqrt(dx * dx + dy * dy);
          if (dist < 100) {
            d.y -= (100 - dist) * 0.02;
          } else {
            d.y += (Math.random() - 0.5) * 0.1;
          }
        });
      });

      function animate() {
        c.fillStyle = "rgba(0, 0, 0, 0)";
        c.clearRect(0, 0, ctx.width, ctx.height);
        dots.forEach((d) => {
          c.fillStyle = "#5865f2";
          c.beginPath();
          c.arc(d.x, d.y, d.size, 0, Math.PI * 2);
          c.fill();
        });
        requestAnimationFrame(animate);
      }
      animate();
    </script>
  </body>
</html>
